import {NextResponse} from "next/server";import {workerReadiness} from "@/lib/workers";
export function GET(request:Request){const secret=process.env.CRON_SECRET;if(!secret||request.headers.get("authorization")!==`Bearer ${secret}`)return NextResponse.json({error:"Unauthorized"},{status:401});const correlationId=crypto.randomUUID();return NextResponse.json({ok:true,correlationId,startedAt:new Date().toISOString(),workers:workerReadiness().filter(w=>w.schedule==="daily").map(w=>({code:w.code,result:"scheduled_for_data_adapter"})),warning:"No database adapter is configured in this repository; no records were mutated."},{headers:{"Cache-Control":"no-store"}})}
