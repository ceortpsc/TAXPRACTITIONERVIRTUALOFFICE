import Link from"next/link";import{requireIdentity}from"@/lib/identity";import{serviceDepartments,serviceReadiness}from"@/lib/student-services";
export default async function StudentServicePortal(){const principal=await requireIdentity();return <main><section className="pageHero"><p className="eyebrow">SECURE STUDENT SERVICE CENTER</p><h1>Cases, messages, appointments, and referrals.</h1><p className="lede">Signed in as {principal.email??principal.subject}. Access remains limited to the authenticated user and authorized staff within the active organization.</p></section><section><div className="heading"><div><p className="eyebrow">MY SERVICES</p><h2>Accountable support across the student lifecycle.</h2></div><p>Demonstration interface only. Secure case persistence and outbound communication are not configured.</p></div><div className="modules">{serviceDepartments.slice(0,8).map(d=><article key={d.code}><span>{d.code}</span><h3>{d.name}</h3><p>{d.purpose}</p><p>Target acknowledgement: {d.sla}</p></article>)}</div></section><section className="dark"><h2>System status</h2><p>{serviceReadiness.reason}</p><div className="actions"><Link className="button" href="/learn">Learning workspace</Link></div></section></main>}
