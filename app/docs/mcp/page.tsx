export default function McpDocumentation() {
  return <main><section className="hero"><p className="eyebrow">SECURE INTEGRATION</p><h1>Clerk-protected MCP</h1><p className="lede">The Virtual Office exposes a read-only Model Context Protocol endpoint protected by Clerk OAuth access tokens and explicit scopes.</p></section><section><h2>Endpoint</h2><p><code>POST /api/mcp</code> using Streamable HTTP and JSON-RPC 2.0.</p><h2>Required scopes</h2><p><code>mcp:connect</code> is required for every request. Tool discovery requires <code>tools:read</code>; tool execution requires <code>tools:execute</code>; resource access requires <code>resources:read</code>.</p><h2>Data boundary</h2><p>The connector returns only non-secret readiness and masked authorization records. Taxpayer records, credentials, raw transcripts, and document-vault content are not exposed.</p></section></main>;
}
