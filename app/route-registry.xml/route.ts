import {publicRoutes} from "@/lib/public-routes";
export const dynamic="force-dynamic";
const esc=(v:string)=>v.replace(/[<>&'\"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]!));
export function GET(){const routes=publicRoutes.map(r=>`  <route path="${esc(r.path)}" kind="${r.kind}" operational="${r.operational}">\n    <title>${esc(r.title)}</title>\n    <description>${esc(r.description)}</description>\n    <readiness requiredStatus="200" contentType="${r.kind==="xml"?"application/xml":r.kind==="api"?"application/json":"text/html"}"/>\n  </route>`).join("\n");const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<routeRegistry xmlns="https://rosstaxsoftware.com/schemas/route-registry/v1" version="1.0" generated="2026-09-05">\n  <application id="tax-practitioner-virtual-office" framework="Next.js" deployment="production-ready"/>\n${routes}\n</routeRegistry>\n`;return new Response(xml,{headers:{"Content-Type":"application/xml; charset=utf-8","Cache-Control":"public, max-age=300, stale-while-revalidate=3600","X-Content-Type-Options":"nosniff"}})}
