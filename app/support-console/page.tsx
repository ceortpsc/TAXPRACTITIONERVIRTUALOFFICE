import Link from "next/link";import {UserButton} from "@clerk/nextjs";import {requireIdentity} from "@/lib/identity";import SupportConsole from "./SupportConsole";
export const metadata={title:"Enterprise Support Console"};
export default async function SupportConsolePage(){const p=await requireIdentity();return <main><header><b>ROSS TAX PRO · ENTERPRISE SUPPORT</b><nav><Link href="/office">Virtual Office</Link><Link href="/settings">Controls</Link><UserButton/></nav></header><section className="pageHero supportHero"><p className="eyebrow">CUSTOMER CARE + IT SERVICE DESK</p><h1>Resolve with discipline.<br/><em>Escalate with evidence.</em></h1><p className="lede">Authenticated operator: {p.email??p.subject}. Roles: {p.roles.join(", ")||"pending assignment"}.</p></section><SupportConsole/></main>}
