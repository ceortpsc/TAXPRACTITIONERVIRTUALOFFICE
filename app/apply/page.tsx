import Link from "next/link";
import { academicPrograms } from "@/lib/academic-catalog";
import { releaseReadiness } from "@/lib/academic-operations";
export const metadata={title:"Academic Application | Ross Tax Pro University",description:"Program interest and admissions-readiness interface."};
export default function ApplyPage(){const readiness=releaseReadiness();return <main><section className="pageHero"><p className="eyebrow">ADMISSIONS</p><h1>Start with clarity.<br/><em>Proceed only when authorized.</em></h1><p className="lede">This interface presents program-interest fields and admissions workflow requirements. Application transmission and degree enrollment are disabled until authority, identity, database, privacy, and program gates pass.</p></section><section><div className="heading"><div><p className="eyebrow">PROGRAM INTEREST</p><h2>Application data map.</h2></div><p>No sensitive information is submitted from this readiness preview.</p></div><form><label>Program<select name="program" disabled><option>Select a proposed or non-degree pathway</option>{academicPrograms.map(p=><option key={p.slug}>{p.title}</option>)}</select></label><label>Entry term<select name="term" disabled><option>Term published after authorization</option></select></label><label>Applicant email<input name="email" type="email" disabled placeholder="Enabled after privacy and identity gates"/></label><button className="button primary" disabled>Applications not yet open</button></form><div className="modules">{readiness.gates.map(g=><article key={g.code}><span>BLOCKING GATE</span><h3>{g.name}</h3><p>Required for: {g.requiredFor.join(", ")}. No external transmission is enabled.</p></article>)}</div><Link className="button" href="/universities/academics">Review pathways</Link></section></main>}
